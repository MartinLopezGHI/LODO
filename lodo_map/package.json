{
  "name": "lodo_map",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:web-public": "pnpm -C apps/web-public dev",
    "dev:web-internal": "pnpm -C apps/web-internal dev",
    "dev:api": "pnpm -C services/api dev",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}